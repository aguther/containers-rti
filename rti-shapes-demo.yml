---
- hosts:
    - master[0]
  vars:
    helm_install_namespace: rti-shapes-demo
    helm_install_chart: https://github.com/aguther/helm-charts/raw/master/rti-shapes-demo-1.6.0.tgz
    helm_install_wait: no
    helm_install_set:
      - key: persistenceService.enabled
        value: no
  roles:
    - helm/install
