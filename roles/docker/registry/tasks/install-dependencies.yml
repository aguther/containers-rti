---
- name: install epel-release
  package:
    name: "{{ item }}"
    state: present
  with_items:
    - epel-release

- name: install package dependencies
  package:
    name: "{{ item }}"
    state: present
  with_items:
    - python-docker-py
    - python-passlib
    - python-bcrypt
