---
# ------------------------------------------------------------------------------
- name: start
  command: "docker -H tcp://{{ groups['master'][0] }}:2400 run --detach=true --net=weave --name rti-perftest-pub-{{ inventory_hostname | checksum }} guther/rti-perftest -pub -numSubscribers {{ groups['nodes'] | length }} -sleep 1000"
...
