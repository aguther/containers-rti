---
# ------------------------------------------------------------------------------
- name: log
  command: "docker -H tcp://{{ groups['master'][0] }}:2400 logs --tail 5 rti-perftest-pub-{{ inventory_hostname | checksum }}"
  register: docker_log

- debug: msg="{{ docker_log.stdout }}"
...
