---
# ------------------------------------------------------------------------------
- include_tasks: network-policy.yml

- include_tasks: subscriber.yml
  vars:
    rti_perftest_id_subscriber: "{{ item }}"
  with_sequence: count={{ groups['nodes'] | length }}

- include_tasks: publisher.yml
