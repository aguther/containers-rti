---
- include_role:
    name: kubernetes/get-status
  tags:
    - kubernetes
    - kubernetes-init

- include_tasks: init.yml
  when: ('masters' in group_names)
  tags:
    - kubernetes
    - kubernetes-init

- include_tasks: join.yml
  when: ('masters' in group_names)
        or ('nodes' in group_names)
  tags:
    - kubernetes
    - kubernetes-join
